添加常量:每平方像素该生成多少粒子
添加常量:粒子最小/最大大小
添加常量:最大/最小随机速度
添加常量:最小/最大粒子生命周期

// 参考用粒子系统常量
	PARTICLES_PER_SQUARE_PIXEL = 0.000005; // 每平方像素生成的粒子数
	PARTICLE_MIN_SIZE = 1;
	PARTICLE_MAX_SIZE = 2;
	PARTICLE_MIN_SPEED = 0.1;
	PARTICLE_MAX_SPEED = 0.5;
	PARTICLE_MIN_LIFETIME = 3000; // 毫秒
	PARTICLE_MAX_LIFETIME = 8000; // 毫秒

粒子为白色圆形
粒子有随机生命周期，时间越接近生命周期粒子越大同时越透明
粒子离屏幕外5个像素生成，随机直线移动，初始速度随机，速度不会变动
被判定移动不会进入屏幕内的粒子被移除